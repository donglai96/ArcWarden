# D120-RSM (2026-08-04, RSM_TEST_PLAN.md Phase 4): the paired RSM arm on the D120 source.
# = giant_x4_d120_ctrl.ini + [rsm] enable k1=0.16 — the ONLY physics delta
# (ctrl already carries the same Ly; same seed/binary/tile_sort/length).
# Pair verdict vs build/d120_ctrl (t6000, complete) via armu_gap_verdict.py:
# user-approved source 2026-08-03: D120 riser crosses 0.5wce (spectral power;
# ridge-peak metric read 0.491 — user judged the crossing from the spectrum).
# = giant_x4_u2band_ctrl.ini with nh 0.0178->0.0120 (cold_nc 0.9880), ppc
# 28000->36000, nsteps->200000 (tOe=6000). A=1.5 kept -> linear band still
# to 0.60; total drive lowered. Question: clean rising-tone ridge crossing
# 0.50 with an upper shoulder at 0.55-0.60, no broadband burst?
# ARM U (2026-07-31): TWO-BAND-CAPABLE f0 CONTROL (no k_perp; the gapless-prediction arm).
# Base = giant_x4_atmo40_rsm.ini with ONE physics delta: uth_perp 0.2439 ->
# 0.31283 (T_perp/T_par = 2.5, A = 1.5 -> cyclotron marginal frequency
# A/(1+A) = 0.60 We0). The x4 f0 (A ~ 0.75-0.88 maintained) capped linear
# growth at 0.43-0.47 -> upper band FORBIDDEN by construction (user caught
# this); with A = 1.5 the linear spectrum spans ~0.2-0.6 CONTINUOUSLY
# (gapless, as in our Li2019 repro) and the question becomes the real one:
# does the m1 Landau channel carve the 0.5 valley between the two bands?
#   PREDICT ctrl (no k_perp): filled 0.2-0.6 spectrum, NO gap.
#   PREDICT rsm: plateau at v_par ~ 0.087-0.100c (BETWEEN upper-band
#   cyclotron ~0.065c and lower-band cyclotron 0.15-0.26c populations)
#   -> two bands + 0.5 valley.
# Runner: ./chirp2d <this> <outdir> --ckpt=111111 --noeline
# k_perp oblique channel (docs/X4_RSM_EPAR_PLAN.md; design in
# docs/RSM_KPERP_IMPLEMENTATION.md). Base = decks/giant_x4_atmo40.ini
# (discrete rising-tone element train, steady period T_b/2, A rises to +0.86)
# with three deltas ONLY:
#   Ly = 2*pi/k1 = 39.27 (RSM phase contract; Ly is inert for the flat path,
#     so the existing build/giant_x4_atmo40 run remains the free control)
#   [rsm] enable, k1 = 0.16 -> WNA theta(w) = atan(k1/kpar): 15.5 deg at
#     w = 0.25 We0 (chirp onset, kpar = 0.577) -> 9.1 deg at 0.5 We0
#   tile_sort dropped (rsm code-guard: flat deposit path only)
# Question: do the self-generated chirping elements Landau-carve a v_par
# plateau at |v| in [0.087, 0.100] c (E_par lives ONLY in m = 1), and does a
# 0.5 fce feature appear in the P1 modal spectrum? Judge from separate
# P0/P1 spectra at the EQUATOR probe, peak-local (V6 rule).
# Runner: ./chirp2d <this> <outdir> --ckpt=25000 --noeline

[grid]
nx = 19392
ny = 1
Lx = 5041.92
Ly = 26.179938779915     # = 2*pi/k1 (RSM phase contract, k1=0.24)

[time]
dt = 0.15
nsteps = 100000          # tOe = 3000 (user 08-04: short run; element lives
                         # t~800-2000, verdict window [500,2500] covered)

[field]
model = yee
jfilter = 3
tile_sort = 25           # RSM tiled deposit path (2026-07-31): shared m0
                         # tiles + 1-D complex m1 tiles; was the 8x slowdown

[plasma]
qm = -1.0
cold_nc = 0.9880
rel = true
noisy = true
seed = 20260720

[background]
wce = 0.2
profile = dipole
lre = 3326.26            # x4 compressed = 2.5x the x10 baseline 1330.504

[boundary]
x = atmo
nd = 500
numax = 0.1
batm = 7.4057            # = b_wall: wall IS the loss cone (all touchers
                         # precipitate; consistent with the conecut load)

[antenna]
amp = 0.0

[species hot]
density = 0.0120
ppc = 36000            # 698M markers (~23 GB) at nx=19392
uth = 0.19783565 0.31283 0.31283
rep = fullf
dist = conecut
cone_b = 7.4057          # alpha_LC,eq = 21.6 deg

[diagnostics]
# lat:     -35     -30     -25     -20     -15    -12.5  -10    -7.5   -5     -2.5  0    (x4 arcs)
probes = -2186.5 -1852.3 -1522.7 -1201.1 -888.9 -736.5 -586.2 -437.8 -291.0 -145.2 0.0 145.2 291.0 437.8 586.2 736.5 888.9 1201.1 1522.7 1852.3 2186.5

[rsm]
enable = true
k1 = 0.24                # Phase-5 k1 scan: 22 deg WNA at chirp-onset kpar ~ 0.58
