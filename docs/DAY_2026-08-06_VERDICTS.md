# 2026-08-06 执行日报:A0 收官 / A-cal PASS / G2 s1 FAIL / A1 pilot 中期

计划源头:docs/PLAN_TWO_TRACK.md v2.1(执行顺序第 2–5 步在今日完成或推进)。
提交:f9ed880(A0 机制包)、b4cb531(A-cal)、2ddd73f(G2 判定)。
Run 数据全部保留于 build/:expG2_ctrl_r3、acal_{a05,a10}、a1_a05(a1_a10 进行中)。

---

## 1. A0 机制诊断包 — 完成(f9ed880)

chirp2d 新增 `--fvdiag`(全部只读 gather,不碰模拟状态;关 = 零内核):

| 产物 | 内容 | 节奏 |
|---|---|---|
| fvline_*.bin | 赤道窗 \|i−i_eq\|<150 **cells** 的 w-加权 f(v∥),600 bins | 每 2000 步 |
| fv2d_*.bin | 同窗粗 (v∥,v⊥),240×120 | 每 10000 步 |
| wl_*.bin | m=0 J·E 功账本,Landau/cyclotron × 16 x-区 × 240 v∥-bin | 采样每 20 步,dump+reset 每 2000 步 |

- m=0 纯度:chirp2d 的 Yee E 不含 m=1(rsm 场在谱数组)→ wl 天然是 m0 账本。
- 开销:k_workledger 实测 4× push(double-atomic 瓶颈)→ wl_acc=20(0.5Ωe 处
  仍 ≥21 采样/周期),机制 run 开销 ~13–19%,仅机制 run 打开。
- 非回归:开/关同 seed WE/WB 包络比 1.000000/1.000005(rerun 噪声级)。
- 定位:诊断级(v∥ 结构与符号翻转);能量闭合归 rsm m1ledger(97f1530)。

## 2. A-cal — PASS(b4cb531)

decks/giant_x4_acal_{a05,a10}.ini(= d120_ctrl + 天线 x0=9696/σ=4/w0=0.05/
trmp=250/toff=1000,t2400/wpe 截止赶在 D120 自燃 element ~2500/wpe 前)。

**结果(±145.2 探针几何均值,post-off 波列通过窗)**:

| amp(电流幅度) | δB_trig/B0 |
|---|---|
| 5e-5 | **7.4e-4** |
| 1e-4 | **1.46e-3** |

- 线性度:均值比 1.985 vs 2.000,偏差 0.8% PASS(bar 10%);逐探针 ±17% =
  同 seed 热背景与波包的相干干涉(~2N/A),±探针几何平均一阶消除。
- 带内噪声底 ~4e-5;近场(赤道探针)只作 flagged 参考,未引用。
- 附带发现:波包外传途中被对流放大 2e-4→6.8e-4(t2100/wpe)——D120 介质
  对 0.25Ωe 有增益。
- 两个测量陷阱(已入 scripts/acal_verdict.py 注释):①解调旋向必须按实测
  ±w0 谱线定(z=By+iBz 载波在 +w0;猜错低测 30×);②通过窗 =
  [trmp,toff]+x/vg,不是 toff+x/vg(错窗测尾巴 → 45% 假非线性)。

## 3. G2 s1 源关卡 — FAIL,Track B 搁置(2ddd73f)

expG2_ctrl_r3:t10000/Ωe 完整,seed 1,~2.9h。三条独立证据:

1. **ω_max = 0.491**(ridge 逐帧最大 @t5549;分位 50/90/99 =
   0.327/0.456/0.491)——从未过 0.5,bar ≥0.55 FAIL;与 G1 ctrl 系
   (0.511/0.470)同在种子方差包络内 → pancake 对 riser 无助益。
2. **上肩不存在**:0.55–0.60 在所有窗(含 element-top [4500,6000],排除
   时间稀释)、所有探针(eq/±145/±291)≤1.4× 噪声(bar ≥10×)。
3. **0.60–0.95 扩展扫描全空**——pancake 独立上带(设计边际 0.83)任何
   频段都未出现。

判定脚本的 "V=0.398 natural valley" 为公式误报(up 9.4e-7 < 噪声 2.1e-6,
双肩假设不成立,真实形状是单调下降坡)——与 G1 "P_gap<P_up" 同类噪声底
陷阱,再次确认 S_B 需 up/hi≥10 前置。

**失败机制(定量)**:引擎 A=0.52 → 边际频率 0.342,其上整段被引擎回旋
阻尼,且引擎密度 5×。净线性增长率符号估算:
ω=0.6:pancake +0.0035·0.55·1.4 ≈ +0.0027,引擎 −0.0178·0.95·0.39 ≈ −0.0066
→ 净负;ω=0.7 更糟(+0.0023 vs −0.0094)。
结构性教训:**ArmU 型双带成立靠单种群 A=1.5(边际 0.6,无自阻尼冲突);
两种群方案要活须 n2≳3×(~0.01,不再是少数种群)或提高引擎 A。**

按预承诺:s1 FAIL → 不跑 s2/s3、不配对。重启设计权在用户,选项:
(a) n2≥0.01;(b) 引擎 A 提升(实质回 ArmU 单种群);(c) 放弃双带 consequence,
Track A 单线。

## 4. A1 pilot — 中期(a05 完;a10 进行中)

decks/giant_x4_a1_{a05,a10}.ini,t4000/Ωe,`--fvdiag` 全开(机制包首次生产
使用)。对照 = **同 seed(20260720)** 未触发 d120_ctrl(ant_amp=0)→ 唯一
系统差异 = 触发(t>2000 后受 float-atomic 混沌限制)。

a05(δB=7.4e-4)vs ctrl,t≤4000/Ωe(scripts/a1_pilot_verdict.py,onset 用
绝对阈值 3e-3 > 触发包 1.5e-3,防 20%-of-max 假对齐):

| 指标 | ctrl | a05 |
|---|---|---|
| eq onset | 1024/Ωe | 883/Ωe |
| 各探针 onset | 914–1011 | 921–1030(互有先后)|
| 峰值 \|Bw\|/B0 | ~1e-2 | 相同 |
| ridge wmax / q99 | 0.593 / 0.532 | 0.511 / 0.511 |

**中期读数**:
- 7.4e-4 触发对 D120 自燃源(t~900/Ωe 点火)基本**隐形**:onset 无系统性
  提前(±100/Ωe 互有正负),元素结构未见更有组织,幅度不变。
- ctrl/a05 的 wmax 差(0.593/0.511)量级 = burst 后同 seed 混沌,不可归因。
- 谱图形态比 Phase-2 "干净 2-riser" 印象更宽带/多子元素——将影响 Track A
  ω_stop 三窗测量的信噪,A2 需正视。
- 早期(t≤900)触发臂 ridge 先爬(0.368 vs 0.266)= 触发包确实被放大并
  chirp,但没抢到点火控制权。

**待 a10(1.46e-3,2×)定稿**:若同样隐形 → pilot 结论 = 幅度阶梯需上探
(A-cal 线性 PASS,外推 amp=2e-4→δB~3e-3 可靠)或改触发时序(更早/更强/
双频),或降级"触发 = 相位参考而非点火控制"。

## 5. 明日/下一步

1. a10 完跑 → A1 pilot 正式判定(本文档 §4 补齐 + 图);
2. fvdiag 首批机制数据 quick-look(fvline 平台斜率 + wl Landau/cyclotron
   符号图)——机制链 P_L1→plateau→P_C0 翻转的首次实测检验在 A3 配对时做;
3. Track B 重启与否 = 用户决定(§3 三选项);
4. Track A 幅度设计复审后进 A2(3-seed ctrl 全长)。

---

## 4b. A1 pilot 正式判定(a10 完跑后;task #20 关闭)

a10(δB=1.46e-3)新增证据:

- **±291 onset = 496/515/Ωe**(ctrl 1011/927,a05 968/1030)——被介质对流
  放大过 3e-3 阈的**触发波包本身**,不是提前的 element:早窗 [350,800] 谱
  内容准单色 0.25–0.29(轻微上移),**未转成上升调**(fig
  a1_trigger_packet_spec.png,6 面板 ±291 对比)。
- 赤道 onset 864(a05 883,ctrl 1024):~150/Ωe 的提前,无幅度序
  (a10 ≈ a05),不构成点火控制。
- 峰值幅度三臂相同(~1e-2);触发臂 wmax(0.511/0.491)≤ ctrl,量级 =
  burst 后混沌。

**pilot 结论:当前设计(赤道 0.25Ωe、toff=200/Ωe、δB≤1.5e-3)下,触发
对 D120 自燃源(t~900/Ωe 点火)不构成点火控制;触发包被放大但离开源区,
且保持单色。**

## 4c. 更重的发现:D120 源本身过不了 0.55 bar(nwin 纪律审计)

d120_ctrl(未触发,全长 t6000)eq ridge 按计划 nwin={1024,1536,2048} 收敛
检查:wmax = 0.593/0.518/0.491(散布 0.10 >> 0.01 bar)→ 0.593 是
nwin=1024 斑点;**收敛结论 = ridge 顶在 0.49–0.53**。q90 = 0.491/0.491/0.460。

→ **当前手上没有任何源在 nwin 纪律下稳健越过 0.55**:
D120 0.49–0.53;G1 s1 0.511 / s2 0.470(种子脆);G2 0.491(FAIL);
D140 非单调;D178 宽带。**0.55 bar 与现有源家族不可同时成立。**

## 5b. 明日决策点(用户)

1. **bar 或源二选一**:(a) 放宽 Track A ctrl bar 至"nwin 收敛后过 0.50"
   (D120 现成合格,今天即可进 A2);(b) 保 0.55 → 需要新源筛查轮
   (引擎 A 阶梯 uth_perp>0.31 @ n=0.012,或其他家族);
2. 触发路线:升幅度(amp 2e-4 → δB~3e-3,已非"弱触发")/ 改时序 / 降级
   为相位参考 —— 仅当 (1) 解决后才有意义;
3. Track B 三选项不变(§3)。

## 6. A-阶梯种子门终判(晚间)+ 走廊分析

- **A20 晚期 riser = 种子偶然(1/3,FAIL)**:s2/s3(seed 20260721/22)均只有
  早期 burst,无晚期孤立元素(晚窗 t>2500 峰值 3.3/2.9e-3 = burst 尾,s1 为
  7.3e-3 真元素);fig a20_seed_gate.png。引擎-A 阶梯路线关闭。
- **机制层面这不再意外**:docs/WHY_NO_060_ELEMENTS.md(95eb62c)的 Omura
  走廊分析表明,暖平行(u∥=0.198)引擎家族的走廊在低频端大开 → 自由能在
  0.2–0.45 烧光,ω_stop≈0.5 内禀;抬 A 只加剧低频 burst。0.55+ 需要
  **冷平行高 A 单种群(Lu-morph)** 或 x10 盒复刻 Lu 的高通条件。
- 今日总账:G2 pancake 死(引擎阻尼乘客)→ A1 触发隐形 → 0.55 bar 现有源
  全灭(nwin 审计)→ A-阶梯证伪 + 种子脆 → 走廊分析给出机制统一解释与
  下一代源设计。**明日第一决策:Lu-morph 梯级(u∥,A)=(0.12,5)/(0.10,6)
  @x4,或同 f0 @x10 盒(lre 1330≈Lu 1131)。**
