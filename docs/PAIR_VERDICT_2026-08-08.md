# Phase-4 配对判定(2026-08-08,冻结工具 lurepro_pair_verdict.py @5ab9552)

三对预承诺全跑全判(ctrl 复用归档 t6000;RSM = +[rsm] k1=0.32, Ly=2π/k1,
t6000,同 binary;半球预注册 SOUTH/SOUTH/NORTH,判定前从 ctrl Poynting
复算一致)。判据 = PLAN_LUMORPH §4 + 修正案 A5,判定工具在任何正式臂数据
存在之前冻结提交。

## 正式结果:0/3 PAIR PASS

| 门 | s1 | s2 | s3 | 通过数 |
|---|---|---|---|---|
| Δω_stop ≥ 0.04 | **+0.0409** P | +0.0273 F | **+0.0682** P | 2/3 |
| S_A < 0.5(ctrl 元素窗,局地带)| 0.614 F | 0.564 F | 1.588 F | **0/3** |
| 下带存活 | P | P | P | 3/3 |
| ctrl→rsm 端点 | 0.695→0.654 | 0.614→0.586 | 0.709→0.641 | |
| S_A[full](记录)| 0.463 | 0.529 | 1.841 | |
| rsm amp B0(5°)(记录)| 6.99e-3 | 6.03e-3 | 7.60e-3 | |
| R_m 元素窗(记录)| 0.293 | 0.409 | 0.336 | |
| W10 rsm(记录)| 0.123 | 0.184 | 0.164 | |

**预注册成功标准未达成;phase-4 配对 = FAIL。** 幅度全部健康(未垮)、
R_m 全程保持弱通道区(校准预测 0.305 兑现),失败不是数值事故。

## 形态学发现(核心机制信息)

1. **稳健效应 = 端点削蚀(top truncation),不是 0.5 挖谷**:三 seed 端点
   一致下移 +0.027~+0.068;s1/s2 的 P_rsm/P_ctrl 比值谱谷底在 0.62–0.65
   Ωe,eq = "ctrl 到得了、rsm 到不了"的 riser 顶部区间,不在任何 0.5-局地
   候选线(0.500/0.517/0.540)上;s3 更极端——比值谱 0.4–0.7 无谷,
   barrier 带反而增强(R_barrier=1.14)但端点仍被截 0.068。
2. **S_A 失败对频带口径稳健**(赤道带/5°局地带/7.5°局地带三种参考,
   s1: 0.75/0.61/0.78,s2: 0.60/0.56/0.53,全部 ≥0.5)。文献口径核查
   (ke2022jgr/Li2019/Ke2017 原文):**报告层 gap = 0.5×赤道 fce**,机制层
   条件是局地的;我们所有谱图单位正确。
3. **S_A 分母被压的结构**(s1/s2):下带被压 0.5–0.7× 主要来自 rsm 臂
   晚期(t>2300)活动缺失,而非主元素受损——主元素完整、幅度相当。
4. 与 D120 教训同构:"ridge-top truncation ≠ visible gap"(用户 08-04 判语)
   ——但这次是在一个稳健跨 0.55 的源上(4/4 seeds)、实测弱通道 R_m≈0.3
   下得到的,排除了"源不够高"与"通道太强(模式竞争)"两个既往嫌疑。

## 解读边界(不越权)

- 已证:x10 Lu-repro 源 + 单 k⊥=0.32 弱通道 RSM,在 t6000 内**不产生
  选择性 0.5 gap**;产生的是中等强度的元素顶部截断 + 活动期缩短。
- 未证/待查:m1 的功到底做在哪(fvdiag + m1ledger 机制 run 未跑);
  k1=0.24(R_m 0.85)是否给出更接近"阻断"的形态(但预注册规则将其
  归为模式竞争区,跑它 = 换问题);plateau 是否形成(f(v∥) 未查)。

## 下一步选项(用户裁决)

A. **机制诊断 run**(phase-5 工具就绪):s1 对重跑 --fvdiag + ledger,
   回答"削顶的能量去哪了 / plateau 有没有形成"——不改判定,只补机制链。
B. k1=0.24 对照对(预注册为模式竞争参照,不作主 case)。
C. 接受弱通道单-k⊥ 负结果,回馈 RSM 模型定义(constitution §6.1 风险
   清单第 4 条兑现:单固定 k⊥ 可能不足;multi-m / 独立上带源方向)。
D. 结合 ke2022 观测形态("gap+dots"):削顶型结果本身是否即为 paper
   story 的一部分(RSM 阻止元素冲顶 = dots 的缺失机制)。

数据:build/lurepro_x10_rsm{,_s2,_s3}(各 ~9GB 含 ckpt);图:
build/lurepro_pair_s{1,2,3}_fig.png,lurepro_rm_cal.png。
